DATABASE_URL=

# 46630 = Robinhood Testnet (dev), 4663 = Robinhood Mainnet (production)
NEXT_PUBLIC_ROBINHOOD_CHAIN_ID=46630
# Opsional: override RPC. Kosong = pakai RPC bawaan viem.
NEXT_PUBLIC_ROBINHOOD_RPC_URL=

# Server-only (tanpa prefix NEXT_PUBLIC_). Minimal 32 karakter.
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
SESSION_SECRET=